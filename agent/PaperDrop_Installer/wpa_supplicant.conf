ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country=US

# Leave network blocks empty on the factory image. WiFi is provisioned by
# the PaperDrop agent using the captive portal; credentials are injected at
# runtime when a user joins the setup network.
