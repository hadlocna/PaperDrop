export const AUTH_EXPIRED_EVENT = 'paperdrop:auth-expired';

export const AUTH_FAILURE_MESSAGES = new Set([
    'Access denied: No token provided',
    'Invalid or expired token',
    'Unauthorized'
]);
